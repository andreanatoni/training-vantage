[project]
name = "training-vantage"
version = "1.0.0"
description = "CLI per gestione running, nutrizione e composizione corporea"
requires-python = ">=3.10"

[tool.ruff]
line-length = 100
target-version = "py310"

[tool.ruff.lint]
select = ["E", "F", "I"]
